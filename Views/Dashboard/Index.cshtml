@model List<SSO.Client.VAMS.Models.UserClaimsDto>

<h2>User Claims</h2>

<table>
    <tr>
        <th>Type</th>
        <th>Value</th>
    </tr>
    @foreach (var claim in Model)
    {
        <tr>
            <td>@claim.Type</td>
            <td>@claim.Value</td>
        </tr>
    }
</table>
